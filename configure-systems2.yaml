# Playbook to call multiple roles to configure systems
- name: Configure systems
  hosts: lab-proxy-keepalive1
  become: yes

  tasks:

- name: Call Roles
  hosts: lab-proxy-keepalive1
  roles:
   #- hostname
   #- firewalld
   #- ntp-config
   - motd
